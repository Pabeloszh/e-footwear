export interface ProductProps {
    average_rating: number
    brand: string
    colors: string[]
    date_added: string
    desc: string
    discount_price: number
    for_kids: boolean
    gender: string
    is_reviewed: boolean
    model: string
    price: number
    sizes: number[]
}